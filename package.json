{
  "name": "filecrow",
  "version": "1.0.0",
  "private": true,
  "description": "A peer-to-peer file server for sharing files over a local network.",
  "scripts": {
    "go-build": "CGO_ENABLED=0 go build -trimpath -ldflags='-s -w'",
    "build": "npm run build -w frontend && npm run go-build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/syeero7/filecrow.git"
  },
  "workspaces": [
    "frontend"
  ]
}
